enable_testing()

set(RUNNER_FD "${CMAKE_CURRENT_SOURCE_DIR}/TESTFD.bin")
set(ROMDIR "${CMAKE_CURRENT_SOURCE_DIR}/../CompROM")

add_test(EGB_4EH_CLOSEPAINT
	Tsugaru_CUI
		${ROMDIR}
		-FD0
		${RUNNER_FD}
		-BOOTKEY
		F0
		-SHAREDDIR
		"${CMAKE_CURRENT_SOURCE_DIR}/EGB_PAI"
		-DEBUG
		-UNITTEST
		-USEFPU
)

add_test(EGB_PUTBLOCK___EGB_PUTBLOCKCOLOR
	Tsugaru_CUI
		${ROMDIR}
		-FD0
		${RUNNER_FD}
		-BOOTKEY
		F0
		-SHAREDDIR
		"${CMAKE_CURRENT_SOURCE_DIR}/EGB_PUT"
		-DEBUG
		-UNITTEST
		-USEFPU
)

